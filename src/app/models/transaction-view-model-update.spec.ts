import { TransactionViewNodelUpdate } from './transaction-view-nodel-update';

describe('TransactionViewNodelUpdate', () => {
  it('should create an instance', () => {
    expect(new TransactionViewNodelUpdate()).toBeTruthy();
  });
});
